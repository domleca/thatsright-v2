{
  "name": "thatsright-guide",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/validate.mjs && astro build && node scripts/place.mjs",
    "preview": "astro preview",
    "test": "vitest run"
  },
  "dependencies": {
    "astro": "^5.6.0"
  },
  "devDependencies": {
    "fuse.js": "^7.0.0",
    "vitest": "^3.2.7"
  }
}
